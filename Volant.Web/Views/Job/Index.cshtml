@model IEnumerable<Volant.Web.ViewModel.JobViewModel>

@{
    ViewBag.Title = "Jobs";
}

<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-sm-4">
        <h2>Project list</h2>
        <ol class="breadcrumb">
            <li>
                <a href="@Url.Action("_Navigation", "Shared")">Home</a>
            </li>
            <li class="active">
                <strong>Project list</strong>
            </li>
        </ol>
    </div>
</div>


<div class="row">
    <div class="col-lg-12">
        <div class="wrapper wrapper-content animated fadeInUp">

            <div class="ibox">
                <div class="ibox-title">
                    <h5></h5>
                    <div class="ibox-tools">
                        <a href="@Url.Action("Create", "Job")" class="btn btn-primary btn-xs">Create New Job</a>
                    </div>
                </div>
                <div class="ibox-content">
                    <div class="row m-b-sm m-t-sm">
                        <div class="col-md-1">
                            <button type="button" id="loading-example-btn" class="btn btn-white btn-sm"><i class="fa fa-refresh"></i> Refresh</button>
                        </div>
                        <div class="col-md-11">
                            <div class="input-group">
                                <input type="text" placeholder="Search" class="input-sm form-control"> <span class="input-group-btn">
                                    <button type="button" class="btn btn-sm btn-primary"> Go!</button>
                                </span>
                            </div>
                        </div>
                    </div>

                    <div class="project-list">

                        <table class="table table-hover">
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr>
                                        <td class="project-status">
                                            <span class="label label-primary">@Html.DisplayFor(modelItem => item.jobStatusName)</span>

                                        </td>
                                        <td class="project-title">
                                            <a href="@Url.Action("Detail", "Job")">@Html.DisplayFor(modelItem => item.jobIdentifier)
                                             @Html.DisplayFor(modelItem => item.jobName)</a>
                                            <br />
                                            <small>Deadline @Html.DisplayFor(modelItem => item.endDate)</small>
                                        </td>
                                        <td class="project-completion">
                                           Priority: @Html.DisplayFor(modelItem => item.priority)
                                        </td>

                                        <td class="project-completion">
                                            Project: @Html.DisplayFor(modelItem => item.projectId)
                                        </td>

                                        <td class="project-actions">
                                            <a href="@Url.Action("Details", "Job", new { id = item.jobId  })" class="btn btn-white btn-sm"><i class="fa fa-folder"></i> View </a>
                                            <a href="@Url.Action("Edit", "Job", new { id = item.jobId })" class="btn btn-white btn-sm"><i class="fa fa-pencil"></i> Edit </a>
                                            <a href="@Url.Action("Materials", "Job", new { projectId = item.jobId })" class="btn btn-white btn-sm"><i class="fa fa-tasks"></i> Materials </a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>